# contracts/

Smart contracts (Solidity, Avalanche Sepolia). Estructura reservada — implementación pendiente.

Ver [README.md](../README.md) raíz, sección "ESTRUCTURA DEL REPOSITORIO" para
`Deal.sol` (escrow, incluyendo `createDeposit`/`lockDeposit`/`refundFull`/`splitRelease`)
y `KapsoGateway.sol`.
