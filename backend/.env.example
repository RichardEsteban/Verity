# Backend de demo: corre 100% en memoria, sin base de datos ni APIs externas.
# Estas dos son las unicas variables que el codigo realmente lee.

JWT_SECRET=change-me-in-production
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
